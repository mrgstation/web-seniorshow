var seeNav = false;

// hide plus
// gsap.set("#plus",{alpha:0});
gsap.set("#horizontal-line",{alpha:0});
gsap.set("#vertical-line",{alpha:0});

gsap.set("#menu",{alpha:1});
// gsap.set("#hidden-nav",{alpha:0});
